{
  "name": "numericka-integrace",
  "version": "1.0.0",
  "scripts": {
    "build": "cd function-plot && npm install && cd ../math-js-integral && npm install && cd .. && npx rimraf public/dist && webpack",
    "dev": "webpack-dev-server"
  },
  "devDependencies": {
    "ts-loader": "^9.3.1",
    "webpack-cli": "^5.1.4",
    "webpack-dev-server": "^4.11.0"
  }
}
